{
  "name": "hireflow-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "HireFlow AI — Frontend Dashboard",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "vite": "^6.3.0",
    "vitest": "^3.2.0"
  }
}
